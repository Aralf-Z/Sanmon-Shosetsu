# Sanmon Roadmap

## 🧩 核心模块(Module)

### 资源管理 (Asset)
- [ ] YooAsset停止游戏时"Async operation 'DownloadSchedulerOperation' has been aborted."警告
- [ ] Asset支持Unitask异步加载
- [ ] 一个子功能，Gameobject的内部池化
- [ ] 精灵图、图集加载 -> ui

### 配置系统 (Config)
- [ ] Table Luban 的可视化工具（Unity 参考：[LubanYoki](https://github.com/HinataYoki/LubanKit)；跨平台：Avalonia）
- [ ] 非表格配置（如游戏设置 -> 走Application.persistentDataPath）

### 存档系统 (Save)
- [ ] 确定方案：自研还是使用开源库？
- [ ] 开发期间JSON，运行时压缩为二进制？尽量不加密（精力花销）
- [ ] 实现快速加载与异步存档

### UI系统 (ui)
- [ ] UIHandle的实现

### 网络 (Net)
- [ ] 基于 fantasy 的局域网实现
- [ ] 基于 fantasy 的 P2P 实现
- [ ] 玩家自建服务器部署方案

---

## 🕹️ 游戏系统 (System)
- 待补充

---

## 📝 运行时记录 (Note)
- 待补充

---

## ✨ 实体与逻辑 (Entity)

### 管线系统 (pipline)
- 所有的严格逻辑均走pipline
- pipline处理管线会处理Entity的回调，回调用Effect写

### 效果系统 (Effect)
- 存在默认Effect，用来处理，比如生命值低于0时，默认死亡，有些会重生
- 可以理解成特殊处理，开发初期就很少，到后面就会根据需求逐渐替换为Effect
- 目前使用lua来配置

### 组件系统 (Component)
- Entity的纯数据，只解决数据的存储方式，有就代表有对应的功能
- 受到Function的修改
- 组件存在异步初始化的情况，功能如何想办法访问到安全的数据呢？

### 功能系统 (Function)
- 处理Entity Component的独立模块，也会调用Effect，也是处理特殊节点才用
- 比如动画功能，物理功能等等

---

## 🛠️ 开发工具包 (DevToolKit)

### console panel

- [ ] 日志面板需要优化大量日志输出时超出mesh渲染的问题
- [ ] 写一个引擎日志缓冲器，引擎日志输出频率太高容易卡
- [ ] 重做控制台设置参数，现在只能初始化的时候注入，应变为可修改的
- [ ] 重做建议模块，现在不太智能
- [ ] 适配Tag筛选

### cheat panel

- [ ] 重做单个作弊按钮
  - 左键点击执行
  - 延迟显示描述
  - 描述面板延迟固定
- [ ] 写一个CheatAnalyzer,现在要包装的东西太多了
- [ ] 自定义宏，不支持输入参数，一次性执行的DSL
  - 参数
    - name
    - order
  - 策划可以综合命令，自定义宏文本，编写测试环境，name必填，order默认100
  - 方法宏属性
    - name
    - params
    - order = 500
  - 无参数方法转换为宏，默认排序为1000，按名称排序
  - 位置在UI最上面一排，这个东西不要搞太多了，一种快捷方式；
- [ ] 需要输入参数的单独按钮
  - 参数有多少个就生成多少个inputField，左侧为名称，用代码的名字
  - 参数别名属性，覆盖代码中的名字
    - alias：用"|"分割
- [ ] cheatIgnore属性：转换为cheat时会被忽略掉，这个一般做宏定义的功能性DSL用，比如打印、延迟xx时间等等
- [ ] 执行反馈，成功失败的反馈
- [ ] 右侧可以挂一个简略版的日志记录
- [ ] 历史记录持久化
  - 最近使用，按重复的自动剔除
  - 经常使用，记录命令使用频率，前置频率最高的

### option panel

- [ ] 切换panel时，自动保存修改

### other

- [ ] 视窗三种模式
  - 全屏模式，默认全屏
  - 浮窗模式，640*360的样子，可以拖拽，可以看到简单日志（日志的前几句，悬浮可查看完整单次输入日志），少许的宏（宏收藏功能）
  - 监控模式，悬浮球，可以拖拽，悬浮变为浮窗模式，主要是监控警告和错误的功能

---

## 🧰 Helper (工具)

### 调试工具 (Debug)
- [ ] 绘制基础图形：圆、矩形、球、方体、圆柱、贝塞尔曲线

### 编辑器快捷

- [ ] ToolBar形式的停靠定位按钮
- [ ] 快速启动
- [ ] 资源包加载方式

---

